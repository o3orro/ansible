---
# Task
# - name: Rollback the service
#   ansible.builtin.include_tasks:
#     file: tasks/restore_tasks.yml
#   vars:
#     fw_svcs:
#       - http
#       - https
#     svcs:
#       - httpd
#       - firewalld
#     files:
#       - /var/www/html/index.html
#     pkgs:
#       - httpd
#       - mod_ssl
##############################
# 1) 방화벽 서비스 제거
# 2) 서비스 종료
# 3) 서비스 설정 삭제
# 4) 패키지 삭제
##############################
- name: Remove the FW service - {{ fw_svcs }}
  ansible.posix.firewalld:
    service: "{{ item }}"
    permanent: true
    state: disabled
  loop: "{{ fw_svcs }}"

- name: Stop the Unit - {{ svcs }}
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: stopped
    enabled: false
  loop: "{{ svcs }}"
  ignore_errors: true

- name: Remove the files - {{ files }}
  ansible.builtin.file:
    name: "{{ item }}"
    state: absent
  loop: "{{ files }}"

- name: Remove the packages - {{ pkgs }}
  ansible.builtin.dnf:
    name: "{{ pkgs }}"
    state: absent