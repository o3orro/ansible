---
# Task
- name: Setup the Web Page - {{ file }}
  ansible.builtin.copy:
    content: |
      <pre><h1>
      Hostname: {{ ansible_fqdn }}
      IP: {{ ansible_default_ipv4.address }}
      </h1></pre>
    dest: "{{ file }}"
    mode: '0644'