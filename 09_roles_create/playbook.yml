---
- name: Virtual Hosting
  hosts: webservers
  gather_facts: true
  tasks:
    - name: Virtual Hosting - /var/www/{{ inventory_hostname }}/
      ansible.builtin.include_role:
        name: myvhost
        
